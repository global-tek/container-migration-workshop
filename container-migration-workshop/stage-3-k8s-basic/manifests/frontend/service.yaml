apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: shipit
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30800      # access via http://<node-ip>:30800
  type: NodePort
