# In production the frontend is reached via Ingress, not NodePort.
# Change to ClusterIP so only the Ingress controller can reach it.
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: shipit
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
